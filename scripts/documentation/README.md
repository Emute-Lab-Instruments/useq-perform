# Documentation Script Archive

The scripts in this directory are historical helpers for older documentation-data pipelines.

They are not part of the current npm build or test workflow, and they are not the authoritative source for runtime or product behavior.

Use `docs/REPO_MAP.md` and the live docs in `docs/` for current guidance. Only touch these scripts when a task explicitly targets this archive.
